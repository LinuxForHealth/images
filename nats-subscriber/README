# Linux for Health NATS Subscriber Image

The Linux for Health NATS Subscriber Image is intended for non-production use.

## Build Command
```
docker buildx build \
              --pull \
              --push \
              --platform linux/amd64,linux/s390x,linux/arm64 \
              -t docker.io/linuxforhealth/nats-subscriber:<image version> .
```
