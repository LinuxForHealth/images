# Linux for Health Zookeeper Standalone Image

The Linux for Health Zookeeper Standalone Image is intended for non-production use.

## Build Command
```
docker buildx build \
              --pull \
              --push \
              --platform linux/amd64,linux/s390x \
              -t docker.io/linuxforhealth/zookeeper-standalone:<image version> .
```
